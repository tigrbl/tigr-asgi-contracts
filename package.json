{
  "name": "tigr-contract-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/artifacts-npm",
    "packages/contract-npm"
  ],
  "scripts": {
    "generate": "python tools/generate_all.py",
    "validate": "python tools/validate_yaml.py && python tools/validate_jsonschema.py && python tools/build_manifest.py && python tools/build_checksums.py",
    "check:versions": "python tools/check_versions.py",
    "test": "pytest -q"
  }
}
