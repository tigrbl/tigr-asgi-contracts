# Contract Gap Feature Matrix Placeholders

This file is a placeholder test target for feature rows promoted from `reports/deduped_gap_feature_matrix.csv`.

The rows linked here track missing contract schema and linkage surfaces. They are planning placeholders until concrete schema files, validation tests, and evidence are implemented.
